version: '3.8'

services:
  repomind-ai:
    build: .
    container_name: repomind-ai
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped
